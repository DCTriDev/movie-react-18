import styled from 'styled-components'
import { Input } from 'antd'

const SearchBoxOutline = styled(Input)`
  .ant-input {
    background-color: rgb(51, 51, 51);
    color: #fff;
  }
`

export default SearchBoxOutline
